# promptpile-compress 历史文档

> 状态：Archived / non-normative

本目录保存 2026-08-05 协议边界收敛之前的设计与 implementation plans，仅用于追溯实现过程。

历史文档可能包含已经被取代的目标，例如：

- 在 `promptpile-compress` 内实现 `lookup_archive` / `read_archived_turn`；
- 在 compress package 内加入 MCP retrieval server；
- 把 vector index 放入 archive 目录。

这些内容**不定义当前架构**。当前结论以父目录 `DESIGN.md`、`TODO.md` 以及仓库 `doc/15-contracts/archive-protocol-v1.md` 为准。
